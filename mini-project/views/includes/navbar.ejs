<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">
      <i class="fa-solid fa-car"></i>
      <b><i>CarPooling</i></b>
    </a>

    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <% if(currentUser) { %>
        <li class="nav-item">
          <a class="nav-link nav_Name"><i>Welcome, <%= currentUser.username %></i></a>
        </li>
        <% } %>
        <% if(currentUser && currentUser.role === "driver") { %>
  <li class="nav-item">
    <a class="nav-link feature" href="/createride">Create Ride</a>
  </li>
  <li class="nav-item">
  <a class="nav-link feature" href="/ridestatus">Ride Status</a>
</li>
<% } %>
       <% if(currentUser && currentUser.role === "passenger") { %>
  <li class="nav-item dropdown">
    <a class="nav-link dropdown-toggle nav-link feature" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
      My Rides
    </a>
    <ul class="dropdown-menu">
      <li><a class="dropdown-item" href="/bookedrides?status=active">Active Ride</a></li>
      <li><a class="dropdown-item" href="/bookedrides?status=completed">Completed Rides</a></li>
      <li><a class="dropdown-item" href="/bookedrides?status=cancelled">Cancelled Rides</a></li>
    </ul>
  </li>
<% } %>
        <% if(currentUser) {%>
          <li class="nav-item">
          <a class="nav-link feature" href="/logout" onclick="return confirm('Are you sure you want to logout?')">Logout</a>
        </li>
          <% }  else { %> 
        <li class="nav-item">
          <a class="nav-link feature" href="/login">Login</a>
        </li>
        <li class="nav-item">
          <a class="nav-link feature" href="/signup">SignUp</a>
        </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
